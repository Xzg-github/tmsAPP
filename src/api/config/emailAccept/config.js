import EmailConfigurationConfig from './config1';
import EmailAcceptConfigurationConfig from './config2';
import AcceptLogConfig from './config3';

const config = {
  EmailConfigurationConfig,
  EmailAcceptConfigurationConfig,
  AcceptLogConfig,
  emailBtn:'邮箱配置',
  acceptBtn:'邮箱接收配置',
  logBtn: '接收日志'
};

export default config;
