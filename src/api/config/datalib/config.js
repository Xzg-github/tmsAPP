import transformConfig from './config1';
import standardConfig from './config2';

const config = {
  transformConfig,
  standardConfig,
  transform:'数据转换库',
  standard:'导入API标准库'
};

export default config;
